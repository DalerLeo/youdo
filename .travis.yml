language: node_js
node_js:
  - "stable"


branches:
  only:
      - master

script:
  - "export SOURCE_DIR=`pwd`"
  - "export API_HOST=84.54.97.114:8000"
  - "export NODE_ENV=production"
  -  "yarn install"
  - "yarn run lint"
  - "yarn run build"

cache:
  directories:
    - node_modules

notifications:
